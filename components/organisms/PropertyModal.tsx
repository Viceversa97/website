import Image from 'next/image';
import { Property } from '@/lib/types';
import { Button } from '@/components/atoms/Button';
export function PropertyModal({ property, close, labels }: { property: Property; close: () => void; labels: { contact: string } }) { return <div className="fixed inset-0 z-30 grid place-items-center bg-slate-950/55 p-5"><section className="relative max-h-[90vh] w-full max-w-lg overflow-auto rounded-xl bg-white p-6 shadow-2xl"><button onClick={close} className="absolute right-4 top-3 text-xl">×</button><h2 className="text-xl font-bold">{property.title}</h2><p className="mt-2 text-sm text-slate-500">View detailed information about this property including features, amenities, and pricing.</p><p className="my-3 text-xs text-slate-500">⌖ {property.location}</p><div className="grid gap-5 sm:grid-cols-2"><div className="relative h-52 overflow-hidden rounded-lg"><Image src={property.image} alt="" fill className="object-cover"/></div><div><strong className="text-2xl text-navy">${property.price.toLocaleString()}</strong><p className="mt-1 text-sm">$544 per sq ft</p><h3 className="mt-5 font-bold">Property Features</h3><ul className="mt-2 grid grid-cols-2 gap-2 text-sm text-slate-600">{property.tags.concat(['Parking','Modern Kitchen','Rooftop Access']).map(x => <li key={x}>◉ {x}</li>)}</ul></div></div><div className="mt-5 grid grid-cols-3 rounded-lg bg-slate-50 p-3 text-center text-sm"><span>▣<br/>{property.beds} Bedrooms</span><span>♜<br/>{property.baths} Bathrooms</span><span>↗<br/>{property.area} Sq Ft</span></div><div className="mt-4 grid gap-2"><Button>▣ Schedule Tour</Button><button className="rounded border py-2 text-sm">✉ {labels.contact}</button></div></section></div>; }
