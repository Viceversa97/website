import { Button } from '@/components/atoms/Button';
type Labels = { buy: string; sell: string; invest: string; agents: string; transactions: string; indices: string; language: string; signIn: string; getStarted: string };
export function Header({ labels, onLanguage, onSignIn, onGetStarted }: { labels: Labels; onLanguage: () => void; onSignIn: () => void; onGetStarted: () => void }) { const links = ['buy','sell','invest','agents','transactions','indices'] as const; return <header className="absolute inset-x-0 z-10 flex items-center justify-between px-5 py-5 text-xs text-white md:px-10 lg:px-[7%]"><div className="hidden gap-6 md:flex">{links.map(k => <a key={k} href={`#${k}`}>{labels[k]}</a>)}</div><div className="ml-auto flex items-center gap-3"><button type="button" onClick={onLanguage} className="font-bold">{labels.language}</button><button type="button" onClick={onSignIn} className="font-bold">{labels.signIn}</button><Button onClick={onGetStarted} className="px-3 py-2 text-xs">{labels.getStarted}</Button></div></header>; }
