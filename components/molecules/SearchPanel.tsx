import { Button } from '@/components/atoms/Button';
export function SearchPanel({ search, activeTab, setActiveTab, labels }: { search: (value: string) => void; activeTab: 'buy' | 'sell' | 'invest'; setActiveTab: (tab: 'buy' | 'sell' | 'invest') => void; labels: { buy: string; sell: string; invest: string; search: string } }) {
 const tabs = [{id:'buy',label:labels.buy},{id:'sell',label:labels.sell},{id:'invest',label:labels.invest}] as const;
 return <div className="mx-auto mt-7 max-w-2xl rounded-xl bg-white p-4 shadow-card"><div className="mb-4 flex justify-center gap-2 text-sm">{tabs.map(tab => <button key={tab.id} type="button" onClick={() => setActiveTab(tab.id)} className={`rounded px-5 py-2 font-extrabold transition ${activeTab === tab.id ? 'bg-navy text-white' : 'text-slate-700 hover:bg-slate-100'}`}>{tab.label}</button>)}</div><div className="flex gap-2"><input onChange={e => search(e.target.value)} placeholder="Enter location, property type, or keyword..." className="min-w-0 flex-1 rounded-lg border border-slate-200 px-4 py-3 text-sm font-medium text-ink placeholder:text-slate-400 outline-navy"/><Button>{labels.search}</Button></div><div className="mt-3 flex items-center gap-3 text-xs text-slate-600"><span>Price Range</span><input aria-label="Price range" type="range" className="accent-navy"/><span>$500,000</span><span className="ml-auto">$5,000,000</span></div></div>;
}
