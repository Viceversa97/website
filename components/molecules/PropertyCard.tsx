import Image from 'next/image';
import { Property } from '@/lib/types';
import { Button } from '@/components/atoms/Button';
export function PropertyCard({ property, onSelect, onContact, labels }: { property: Property; onSelect: () => void; onContact: () => void; labels: { view: string; contact: string } }) { return <article className="overflow-hidden rounded-xl bg-white shadow-card"><div className="relative h-48"><Image src={property.image} alt={property.title} fill className="object-cover"/><span className="absolute left-3 top-3 rounded bg-navy px-2 py-1 text-[10px] font-bold text-white">{property.type}</span><strong className="absolute bottom-3 left-3 rounded bg-white px-3 py-2 text-lg text-navy">${property.price.toLocaleString()}</strong></div><div className="p-4"><h3 className="min-h-10 text-sm font-bold">{property.title}</h3><p className="mt-2 text-xs text-slate-500">⌖ {property.location}</p><p className="mt-2 text-xs text-slate-600">▣ {property.beds} beds &nbsp; ▣ {property.baths} baths &nbsp; ↗ {property.area} sqft</p><div className="mt-3 flex gap-1 overflow-hidden">{property.tags.slice(0,3).map(tag => <span key={tag} className="pill whitespace-nowrap">{tag}</span>)}</div><div className="mt-4 grid grid-cols-2 gap-2"><button type="button" onClick={onSelect} className="rounded border border-slate-200 py-2 text-xs font-semibold">{labels.view}</button><Button onClick={onContact} className="px-1 py-2 text-xs">{labels.contact}</Button></div></div></article>; }
